'use client';
import styled from '@emotion/styled';

export const Card = styled.section({
  backgroundColor: '#11151E',
  boxShadow: '0 2px 4px #0f2c34dd',
  borderRadius: '1.25rem',
  padding: '1.25rem',
});
